// | **Route**             | **Method** | **Description**                           |
// | --------------------- | ---------- | ----------------------------------------- |
// | `/leaderboard`        | `GET`      | Global leaderboard based on `rankPoints`. |
// | `/leaderboard/:tier`  | `GET`      | Leaderboard filtered by `rankTier`.       |
// | `/users/:userId/rank` | `GET`      | Get user’s current rank & stats.          |
